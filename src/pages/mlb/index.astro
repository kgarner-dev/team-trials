---
import Layout from '../../layouts/Layout.astro';
import CategoryCard from '../../components/CategoryCard.astro';
import Hero from '../../components/Hero.astro';

// let arr = [];
// const numbers = new Set();

// while (numbers.size < 10) {
// 	numbers.add(Math.floor(Math.random() * 133) + 1);
// };

// arr.push(...numbers);
---

<Layout title="MLB">
	<div class="container">
        <Hero 
            headline="MLB"
            body="Lorem ipsum dolor sit amet, consectetur adipiscing elit. Phasellus euismod mi elit. In euismod dignissim justo, tincidunt elementum nisl laoreet eu. Nam porta tincidunt neque. Donec egestas justo a turpis rhoncus tincidunt. Ut sagittis eget lacus ut suscipit."
            breadcrumb="← Back Home"
            breadcrumb_link="/"
        />
		<div class="category">
			<CategoryCard 
				title="American" 
				url="/mlb/al"
				logo="https://a.espncdn.com/i/teamlogos/mlb/500/scoreboard/al.png"
				color="#004786"
			/>

			<CategoryCard 
				title="National" 
				url="/mlb/nl"
				logo="https://a.espncdn.com/i/teamlogos/mlb/500/scoreboard/nl.png"
				color="#004786"
			/>
		</div>
	</div>
</Layout>

<style lang="scss">
	@use "../../components/global.scss";

	.container {
		max-width: 1400px;
		margin: 0 auto;
		min-height: 80vh;
		padding: 20px;
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.category {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		grid-template-rows: auto;
		gap: 10px;

		h2 {
			grid-column: auto / span 4;
			font-size: clamp(1.25rem, 2.55vw, 2.55rem);
			margin: 0;
		}

		p, .link {
			grid-column: auto / span 4;
		}

		p {
			margin: 5px 0;
		}

		.link {
			margin: 0 0 20px;
		}
	}

</style>